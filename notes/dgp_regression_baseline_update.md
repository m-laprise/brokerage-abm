# DGP regression-baseline update

The deterministic trajectory targets in `test/test_regression_baseline.jl`
were updated after the approved variance-share matching function and unit
systematic-signal scale were committed in `e83a584`. The new targets record
the intended current model behavior. No tolerance was relaxed.
