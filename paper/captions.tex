% captions.tex: hand-edited source of truth for the results-section figure titles
% and captions. One ptitle and one pcaption block per figure; the name matches the
% \pvtitle{name} / \pvcaption{name} references in section_source.tex (and the
% fig:<name> label). Titles are rendered by the TeX (as the bold lead-in of the
% caption), not baked into the PNGs. Blocks may use \pv{key} references: data
% values come from values.tex (stats.jl), display conventions from figmeta.tex
% (figures.jl), so derivation facts quoted here cannot drift from the figure code.
% Convention: captions describe elements and derivations only, no interpretation.

\begin{ptitle}{position}
Broker centrality and access fraction
\end{ptitle}
\begin{pcaption}{position}
Left: broker betweenness centrality and access fraction over time at the
baseline, as \pv{rollWin}-observation rolling ensemble means over
seeds; centrality is measured every \pv{betwInterval} periods. Right: one dot
per one-at-a-time regime, each a late-window mean over that regime's
seeds. Time starts at $t=\pv{axisStart}$ (end of burn-in).
\end{pcaption}

\begin{ptitle}{grid}
Outcomes across the matching problem ($\rho\times\delta$)
\end{ptitle}
\begin{pcaption}{grid}
Six outcomes across the $\rho\times\delta$ grid; one line per gain
$\delta$, one marker per regime, each a late-window mean over that regime's seeds.
The rank-correlation gap is broker minus principals; the output gap, broker
channel minus self-search.
\end{pcaption}

\begin{ptitle}{advantage}
Prediction and output gaps vs centrality and access
\end{ptitle}
\begin{pcaption}{advantage}
Rank-correlation gap (broker minus principals) and output gap (broker channel
minus self-search) against broker betweenness centrality and access fraction; one
dot per regime, each a late-window mean over that regime's seeds.
\end{pcaption}

\begin{ptitle}{dynamics}
Baseline dynamics
\end{ptitle}
\begin{pcaption}{dynamics}
Lines are \pv{rollWin}-observation rolling ensemble means over seeds at the
baseline regime. Axes start at $t=\pv{axisStart}$.
\end{pcaption}
